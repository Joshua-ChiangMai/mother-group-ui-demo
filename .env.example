# NestJS API base URL (no trailing slash). Used by Vite at build time and in dev.
VITE_API_BASE_URL=https://abqd2pkatkuks607gntnz1rb.lanna.engineer

# Optional: local dev proxy target when VITE_API_BASE_URL=/api (see vite.config.ts)
# VITE_DEV_PROXY_TARGET=http://localhost:3001
